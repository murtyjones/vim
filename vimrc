call plug#begin()

let g:python_highlight_all = 1

call plug#end()

" Change the leave-insert-mode key from escape to jk
inoremap jk <ESC>

" Change the leader key to the space bar
let mapleader = " "

filetype plugin indent on
syntax on
set encoding=utf-8
set clipboard=unnamedplus

